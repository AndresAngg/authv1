<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Iniciar sesión</title>
  <style>
    :root{
      --bg:#0f172a;        /* slate-900 */
      --panel:#0b1220;
      --ring:#334155;      /* slate-700 */
      --text:#e5e7eb;      /* gray-200 */
      --muted:#94a3b8;     /* slate-400 */
      --accent:#3b82f6;    /* blue-500 */
      --accent2:#06b6d4;   /* cyan-500 */
      --danger:#ef4444;    /* red-500 */
      --success:#22c55e;   /* green-500 */
    }
    *{box-sizing:border-box}
    body{
      margin:0; font-family:system-ui,-apple-system,Segoe UI,Roboto,Ubuntu,Cantarell,Arial;
      background: radial-gradient(1200px 800px at 20% 0%, rgba(59,130,246,.15), transparent 40%),
                  radial-gradient(1000px 700px at 100% 100%, rgba(6,182,212,.12), transparent 45%),
                  var(--bg);
      color:var(--text); min-height:100vh; display:flex; flex-direction:column;
    }
    a{color:inherit; text-decoration:none}
    .container{width:100%; max-width:980px; margin:0 auto; padding:16px}
    /* NAV */
    .nav{position:sticky; top:0; z-index:10; border-bottom:1px solid var(--ring); background:rgba(2,6,23,.6); backdrop-filter:blur(8px)}
    .nav-inner{display:flex; align-items:center; justify-content:space-between; padding:12px 0}
    .brand{display:flex; align-items:center; gap:10px; font-weight:700}
    .badge{width:28px; height:28px; border-radius:8px; background:linear-gradient(135deg,var(--accent),var(--accent2)); box-shadow:0 0 0 2px rgba(255,255,255,.05),0 8px 20px rgba(59,130,246,.18)}
    .link{border:1px solid var(--ring); padding:8px 12px; border-radius:10px; font-weight:600; background:var(--panel)}
    .link:hover{border-color:#475569}
    /* LAYOUT */
    main{display:grid; place-items:center; flex:1; padding:24px}
    .card{
      width:100%; max-width:460px; background:linear-gradient(180deg, rgba(255,255,255,.05), rgba(255,255,255,.02));
      border:1px solid var(--ring); border-radius:16px; padding:22px;
      box-shadow:0 12px 40px rgba(0,0,0,.25), inset 0 1px 0 rgba(255,255,255,.04);
    }
    h1{margin:0 0 10px}
    p.muted{color:var(--muted); margin-top:0}
    /* FORM */
    form{display:grid; gap:12px; margin-top:10px}
    label{font-weight:600}
    .input{
      width:100%; padding:11px 12px; border-radius:10px; border:1px solid var(--ring);
      background:var(--panel); color:var(--text); outline:none; transition:border-color .15s, box-shadow .15s;
    }
    .input:focus{border-color:var(--accent); box-shadow:0 0 0 3px rgba(59,130,246,.25)}
    .actions{display:flex; gap:10px; flex-wrap:wrap; margin-top:2px}
    .btn{
      flex:1; text-align:center; border-radius:10px; padding:10px 12px; font-weight:700; cursor:pointer;
      border:1px solid var(--ring); background:var(--panel); color:var(--text);
      transition:transform .06s ease, filter .2s;
    }
    .btn:hover{transform:translateY(-1px)}
    .btn-primary{border:0; background:linear-gradient(90deg,var(--accent),var(--accent2))}
    /* ALERTS */
    .alerts{display:grid; gap:10px; margin:10px 0}
    .alert{padding:10px 12px; border:1px solid var(--ring); border-radius:10px; background:rgba(148,163,184,.08)}
    .alert.error{border-color:rgba(239,68,68,.5); background:rgba(239,68,68,.1)}
    .alert.success{border-color:rgba(34,197,94,.5); background:rgba(34,197,94,.1)}
    footer{border-top:1px solid var(--ring); background:rgba(2,6,23,.6)}
    .foot{padding:14px 0; color:var(--muted)}
  </style>
</head>
<body>
  <nav class="nav">
    <div class="container nav-inner">
      <div class="brand">
        <span class="badge" aria-hidden="true"></span>
        <a href="/"><span>Tu App</span></a>
      </div>
      <a class="link" href="/register">Crear cuenta</a>
    </div>
  </nav>

  <main>
    <section class="card">
      <h1>Iniciar sesión</h1>
      <p class="muted">Usa tu usuario y contraseña.</p>

      <div class="alerts" role="region" aria-live="polite">
        <% if (typeof error !== 'undefined' && error) { %>
          <div class="alert error"><%= error %></div>
        <% } %>
        <% if (typeof success !== 'undefined' && success) { %>
          <div class="alert success"><%= success %></div>
        <% } %>
      </div>

      <!-- Submit clásico: el backend valida y decide (render/redirect) -->
      <form action="/login" id="loginForm" method="POST" novalidate>
        <% if (typeof csrfToken !== 'undefined' && csrfToken) { %>
          <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <% } %>
        <% if (typeof next !== 'undefined') { %>
          <input type="hidden" name="next" value="<%= next %>">
        <% } %>

        <div>
          <label for="username">Usuario</label>
          <input class="input" id="username" name="username" type="text" required minlength="3" autocomplete="username" placeholder="tu_usuario">
        </div>

        <div>
          <label for="password">Contraseña</label>
          <input class="input" id="password" name="password" type="password" required minlength="4" autocomplete="current-password" placeholder="••••••">
        </div>

        <div class="actions">
          <button class="btn btn-primary" type="submit">Entrar</button>
          <a class="btn" href="/register">Registrar</a>
        </div>
      </form>
    </section>
  </main>

  <footer>
    <div class="container foot">© <%= new Date().getFullYear() %> Tu App</div>
  </footer>
</body>
<script>
document.getElementById('loginForm').addEventListener('submit', async (e)=>{
  e.preventDefault();
  const fd = new FormData(e.target);
  const body = { username: fd.get('username'), password: fd.get('password') }

  const r = await fetch('/login', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json', 'Accept': 'application/json' },
    body: JSON.stringify(body),
    credentials: 'include' // importante si el backend coloca el JWT en cookie
  });

  if (r.ok) {
    window.location.href = '/protected'; // 👈 redirect desde la vista
  } else {
    const data = await r.json().catch(()=>({}));
    const box = document.getElementById('err');
    box.textContent = data.error || 'Error al iniciar sesión';
    box.style.display = 'block';
  }
});
</script>
</html>
